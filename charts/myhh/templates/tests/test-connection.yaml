apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myhh.fullname" . }}-test-connection"
  labels:
    {{- include "myhh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myhh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
