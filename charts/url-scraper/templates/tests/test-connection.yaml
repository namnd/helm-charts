apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "url-scraper.fullname" . }}-test-connection"
  labels:
    {{- include "url-scraper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "url-scraper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
