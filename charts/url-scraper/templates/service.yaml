apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-scraper.fullname" . }}
  labels:
    {{- include "url-scraper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "url-scraper.selectorLabels" . | nindent 4 }}
